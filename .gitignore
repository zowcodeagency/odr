node_modules
.env
.env.local
*.log
.DS_Store
dist
build
.turbo
.cache
coverage
.env.production
output
tmp
apps/box/dist/
apps/box/.data/

# Claude Code worktrees
.claude/worktrees/

# Promo video workspace (remotion) — lives outside git; its deps stay in bun.lock so `bun install` is clean
apps/promo/
